# included templates
include:
  - 'https://gitlab.com/senbox-org/snap-engine/raw/master/.gitlab-ci.yml'

# variables
variables:
  MAVEN_IMAGE: "ghcr.io/carlossg/maven:3.9.2-eclipse-temurin-8-alpine"

macos:
  when: never

windows:
  when: never
