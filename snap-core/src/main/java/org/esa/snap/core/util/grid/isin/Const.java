package org.esa.snap.core.util.grid.isin;

class Const {

    static final double TWOPI = 2.0 * Math.PI;
    static final double HALFPI = 0.5 * Math.PI;

    static final double EPS_CNVT = 0.01;    // Doubles must be within this of an integer to be valid
    static final double EPS_SPHERE = 1.0e-10;  // Minimum sphere radius
}
